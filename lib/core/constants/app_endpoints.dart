// lib/core/constants/app_endpoints.dart
// Định nghĩa các endpoint API dùng trong app.
// Lưu ý: không bao gồm domain, chỉ bao gồm path.
// Ví dụ: /api/v1/login
// Domain sẽ được cấu hình riêng tùy môi trường (dev, staging, prod).
// Các endpoint nên được đặt tên theo chức năng, không nên đặt tên theo kỹ thuật (REST, GraphQL, v.v.).
