// Firestore Security Rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // ============================================================================
    // USERS COLLECTION 
    // ============================================================================
    match /users/{userId} {
      // Cho phép đọc nếu người dùng đã đăng nhập và đúng userId
      allow read: if request.auth != null && request.auth.uid == userId;

      // Cho phép tạo user mới khi đăng ký (phải trùng với auth uid)
      allow create: if request.auth != null && request.auth.uid == userId;

      // Cho phép cập nhật thông tin của chính mình
      allow update: if request.auth != null && request.auth.uid == userId;

      // Không cho phép xóa user
      allow delete: if false;
    }
    
    // ============================================================================
    // PERSONAL VOCABULARIES COLLECTION (🆕 THÊM MỚI)
    // ============================================================================
    match /personal_vocabularies/{userId} {
      // User chỉ được đọc/ghi bookmarks của chính họ
      allow read, write: if request.auth != null && request.auth.uid == userId;
        // Validate data structure khi tạo mới
      allow create: if request.auth != null 
                    && request.auth.uid == userId
                    && request.resource.data.keys().hasAll(['id', 'userId', 'vocabularyCardIds', 'createdAt', 'updatedAt'])
                    && request.resource.data.vocabularyCardIds is list;
      
      // Validate khi update
      allow update: if request.auth != null 
                    && request.auth.uid == userId
                    && request.resource.data.vocabularyCardIds is list;
    }
    
    // ============================================================================
    // USER STREAKS COLLECTION (🔥 Streak tracking)
    // ============================================================================
    match /user_streaks/{userId} {
      // User chỉ được đọc/ghi streak của chính họ
      allow read, write: if request.auth != null && request.auth.uid == userId;
      
      // Validate data structure khi tạo mới
      allow create: if request.auth != null 
                    && request.auth.uid == userId
                    && request.resource.data.keys().hasAll(['userId', 'currentStreak', 'longestStreak', 'createdAt', 'updatedAt']);
      
      // Validate khi update
      allow update: if request.auth != null 
                    && request.auth.uid == userId
                    && request.resource.data.currentStreak is int
                    && request.resource.data.longestStreak is int;
    }

    // ============================================================================
    // 🆕 FLASHCARD PROGRESS COLLECTION (📊 Learning progress tracking)
    // Document ID format: {userId}_{topicId}
    // Example: Zgd3MQzrcLX4hKa8SiaPl3fYqs62_business
    // ============================================================================
    match /flashcard_progress/{documentId} {
      // Helper function: Check if document belongs to current user
      function isOwner() {
        return request.auth != null 
               && documentId.matches('^' + request.auth.uid + '_.*');
      }
      
      // Allow read/write if document ID starts with user's UID
      allow read, write: if isOwner();
      
      // Validate data structure khi tạo mới
      allow create: if isOwner()
                    && request.resource.data.keys().hasAll(['userId', 'topicId', 'masteredCardIds', 'learningCardIds', 'lastStudiedAt', 'createdAt'])
                    && request.resource.data.userId == request.auth.uid
                    && request.resource.data.masteredCardIds is list
                    && request.resource.data.learningCardIds is list;
      
      // Validate khi update
      allow update: if isOwner()
                    && request.resource.data.masteredCardIds is list
                    && request.resource.data.learningCardIds is list;
      
      // Allow delete own progress (for reset functionality)
      allow delete: if isOwner();
    }
    
    // ============================================================================
    // DEFAULT DENY ALL (Bảo mật)
    // ============================================================================
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
